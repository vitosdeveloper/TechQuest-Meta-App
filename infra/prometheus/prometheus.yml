global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']
  
  # Aqui entraremos com os jobs dos nossos microservicos (Node.js/NestJS) posteriormente
